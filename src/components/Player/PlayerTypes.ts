

export type aType = {
    href: string,
    name: string,
}

export type PlayerConfigType = {
    prevBtn: aType,
    playBtn: aType,
    nextBtn: aType,
    shuffleBtn: aType,
    loopBtn: aType,
}